--- a/fs/proc/task_mmu.c
+++ b/fs/proc/task_mmu.c
@@ -927,7 +927,7 @@
 #ifdef CONFIG_KSU_SUSFS_SUS_MAP
 	if (vma->vm_file) {
 		if (SUSFS_IS_INODE_SUS_MAP(file_inode(vma->vm_file)))
-			return;
+			return 0;
 	}
 #endif // #ifdef CONFIG_KSU_SUSFS_SUS_MAP
 

--- a/fs/notify/fdinfo.c
+++ b/fs/notify/fdinfo.c
@@ -105,6 +105,31 @@
 	inode_mark = container_of(mark, struct inotify_inode_mark, fsn_mark);
 	inode = igrab(fsnotify_conn_inode(mark->connector));
 	if (inode) {
+#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
+		mnt = real_mount(file->f_path.mnt);
+		if (mnt->mnt_id >= DEFAULT_KSU_MNT_ID &&
+			likely(susfs_is_current_proc_umounted()))
+		{
+			struct path path;
+			char *pathname = kmalloc(PAGE_SIZE, GFP_KERNEL);
+			char *dpath;
+
+			dpath = d_path(&file->f_path, pathname, PAGE_SIZE);
+			if (pathname && !IS_ERR(dpath) && !kern_path(dpath, 0, &path) &&
+			    path.dentry->d_inode) {
+				seq_printf(m, "inotify wd:%x ino:%lx sdev:%x mask:%x ignored_mask:0 ",
+						inode_mark->wd, path.dentry->d_inode->i_ino,
+						path.dentry->d_inode->i_sb->s_dev,
+						mark->mask & IN_ALL_EVENTS);
+				show_mark_fhandle(m, path.dentry->d_inode);
+				seq_putc(m, '\n');
+				path_put(&path);
+			}
+			kfree(pathname);
+			iput(inode);
+			return;
+		}
+#endif // #ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
 		/*
 		 * IN_ALL_EVENTS represents all of the mask bits
 		 * that we expose to userspace.  There is at
@@ -130,7 +155,11 @@
 
 #ifdef CONFIG_FANOTIFY
 
+#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
+static void fanotify_fdinfo(struct seq_file *m, struct fsnotify_mark *mark, struct file *file)
+#else
 static void fanotify_fdinfo(struct seq_file *m, struct fsnotify_mark *mark)
+#endif
 {
 	unsigned int mflags = 0;
 	struct inode *inode;

--- a/fs/namespace.c
+++ b/fs/namespace.c
@@ -30,10 +30,21 @@
 #include <uapi/linux/mount.h>
 #include <linux/fs_context.h>
 #include <linux/shmem_fs.h>
+#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
+#include <linux/susfs_def.h>
+#endif
 
 #include "pnode.h"
 #include "internal.h"
 
+#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
+extern bool susfs_is_current_ksu_domain(void);
+extern struct static_key_true susfs_is_sdcard_android_data_not_decrypted;
+
+#define CL_COPY_MNT_NS BIT(25) /* used by copy_mnt_ns() */
+
+#endif // #ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
+
 
 
 

--- a/include/trace/hooks/sched.h
+++ b/include/trace/hooks/sched.h
@@ -95,7 +95,7 @@
 #define trace_android_rvh_prepare_prio_fork(p)
 #define trace_android_rvh_finish_prio_fork(p)
 #define trace_android_rvh_rtmutex_prepare_setprio(p, pi_task)
-#define trace_android_rvh_set_user_nice(p, nice)
+#define trace_android_rvh_set_user_nice(p, nice, allowed)
 #define trace_android_rvh_setscheduler(p)
 #define trace_android_rvh_check_preempt_wakeup(p, ignore)
 #define trace_android_rvh_check_preempt_tick(p, ideal_runtime)

--- a/include/trace/hooks/vh_af_packet.h
+++ b/include/trace/hooks/vh_af_packet.h
@@ -25,7 +25,7 @@
 		int *do_drop),
 	TP_ARGS(sk, skb, dev, pt, do_drop));
 #else
-#define android_vh_check_dhcp_pkt(sk, skb, dev, pt, do_drop)
+#define trace_android_vh_check_dhcp_pkt(sk, skb, dev, pt, do_drop)
 #endif
 #endif /* _TRACE_HOOK_AF_PACKET_H */
 /* This part must be outside protection */

--- a/kernel/taskstats.c
+++ b/kernel/taskstats.c
@@ -466,7 +466,7 @@
 	}
 	stats->zram_compressed = K(zspages);
 }
-#elif
+#else
 static void sysstats_fill_zoneinfo(struct sys_memstats *stats)
 {
 }

--- a/arch/arm64/configs/defconfig
+++ b/arch/arm64/configs/defconfig
@@ -703,7 +703,7 @@
 CONFIG_TI_SCI_CLK=y
 CONFIG_COMMON_CLK_QCOM=y
 CONFIG_QCOM_A53PLL=y
-CONFIG_QCOM_CLK_APCS_MSM8916=y
+# CONFIG_QCOM_CLK_APCS_MSM8916 is not set
 CONFIG_QCOM_CLK_SMD_RPM=y
 CONFIG_QCOM_CLK_RPMH=y
 CONFIG_IPQ_GCC_8074=y
@@ -850,6 +850,51 @@
 CONFIG_NLS_CODEPAGE_437=y
 CONFIG_NLS_ISO8859_1=y
 CONFIG_SECURITY=y
+# BailinT: kernelsu selinux 集成依赖（r14 同款）
+CONFIG_SECURITY_SELINUX=y
+CONFIG_SECURITY_NETWORK=y
+# BailinT: v5 link-gap 补链（r7 14 undefined 4 组 + 显示/触摸栈）
+CONFIG_IPV6=y
+CONFIG_NET_UDP_TUNNEL=y
+CONFIG_NET_IP_TUNNEL=y
+CONFIG_DST_CACHE=y
+CONFIG_QCOM_SCM=y
+CONFIG_OPLUS_FEATURE_OPROJECT=y
+CONFIG_ARCH_YUPIK=y
+CONFIG_MEDIA_SUPPORT=y
+CONFIG_MEDIA_CAMERA_SUPPORT=y
+CONFIG_VIDEO_DEV=y
+CONFIG_VIDEO_V4L2=y
+CONFIG_V4L2_MEM2MEM_DEV=y
+CONFIG_VIDEOBUF2_CORE=y
+CONFIG_VIDEOBUF2_V4L2=y
+CONFIG_VIDEOBUF2_MEMOPS=y
+CONFIG_VIDEOBUF2_DMA_CONTIG=y
+CONFIG_DRM=y
+CONFIG_DRM_PANEL=y
+# BailinT: DRM_MSM 关闭——lunaa=yupik(SM7325) 无对应 disp conf（techpack/display/config 只有 kona/lahaina/holi/lito/bengal/monaco 段），显示主体本就不编；开 =y 反而拉两套 msm 骨架（drivers/gpu/drm/msm + techpack/display/msm）有重复定义风险
+# CONFIG_DRM_MSM is not set
+# BailinT: techpack/display/msm/Makefile 的 obj-$(CONFIG_DRM_FBDEV_EMULATION) += msm_fbdev.o 是死条目（msm_fbdev.c 不在树上），qgki 官方也关
+# CONFIG_DRM_FBDEV_EMULATION is not set
+CONFIG_DRM_OPLUS_NOTIFY=y
+CONFIG_TOUCHPANEL_OPLUS=y
+CONFIG_TOUCHPANEL_NOTIFY=y
+CONFIG_TOUCHPANEL_ALGORITHM=y
+CONFIG_TOUCHPANEL_CUSTOM=y
+CONFIG_TOUCHPANEL_SYNAPTICS=y
+CONFIG_TOUCHPANEL_FOCAL=y
+CONFIG_TOUCHPANEL_SAMSUNG=y
+CONFIG_TOUCHPANEL_ILITEK=y
+CONFIG_TOUCHPANEL_SYNAPTICS_TCM_ONCELL=y
+CONFIG_TOUCHPANEL_FOCAL_FT3518=y
+CONFIG_TOUCHPANEL_SAMSUNG_S6SY791=y
+CONFIG_TOUCHPANEL_SAMSUNG_S6SY792=y
+CONFIG_TOUCHPANEL_ILITEK_ILITEK7807S=y
+# BailinT: r10 五符号补链（get_boot_mode/register_devinfo/request_firmware_select/msm_drm_*）
+CONFIG_OPLUS_FEATURE_PROJECTINFO=y
+CONFIG_OPLUS_DEVICE_IFNO=y
+CONFIG_OPLUS_FW_UPDATE=y
+CONFIG_DRM_MSM=y
 CONFIG_CRYPTO_ECHAINIV=y
 CONFIG_CRYPTO_ANSI_CPRNG=y
 CONFIG_DMA_CMA=y

--- a/net/mac80211/Makefile
+++ b/net/mac80211/Makefile
@@ -53,14 +53,9 @@
 
 CFLAGS_trace.o := -I$(src)
 
-rc80211_minstrel-y := \
-	rc80211_minstrel.o \
-	rc80211_minstrel_ht.o
-
-rc80211_minstrel-$(CONFIG_MAC80211_DEBUGFS) += \
-	rc80211_minstrel_debugfs.o \
-	rc80211_minstrel_ht_debugfs.o
-
-mac80211-$(CONFIG_MAC80211_RC_MINSTREL) += $(rc80211_minstrel-y)
+mac80211-$(CONFIG_MAC80211_RC_MINSTREL) += rc80211_minstrel.o
+mac80211-$(CONFIG_MAC80211_RC_MINSTREL) += rc80211_minstrel_ht.o
+mac80211-$(CONFIG_MAC80211_DEBUGFS) += rc80211_minstrel_debugfs.o
+mac80211-$(CONFIG_MAC80211_DEBUGFS) += rc80211_minstrel_ht_debugfs.o
 
 ccflags-y += -DDEBUG

--- a/techpack/display/Makefile
+++ b/techpack/display/Makefile
@@ -29,6 +29,18 @@
      endif
 endif
 
+ifeq ($(CONFIG_ARCH_YUPIK), y)
+     include $(srctree)/techpack/display/config/holidisp.conf
+LINUXINCLUDE    += -include $(srctree)/techpack/display/config/holidispconf.h
+export CONFIG_DRM_OPLUS=y
+export CONFIG_DRM_OPLUS_NOTIFY=y
+export CONFIG_V4L2_MEM2MEM_DEV=y
+export CONFIG_VIDEOBUF2_CORE=y
+export CONFIG_VIDEOBUF2_V4L2=y
+export CONFIG_VIDEOBUF2_MEMOPS=y
+export CONFIG_VIDEOBUF2_DMA_CONTIG=y
+endif
+
 LINUXINCLUDE    += \
 		   -I$(srctree)/techpack/display/include/uapi/display \
 		   -I$(srctree)/techpack/display/include

--- a/drivers/gpu/drm/Makefile
+++ b/drivers/gpu/drm/Makefile
@@ -95,7 +95,8 @@
 obj-$(CONFIG_DRM_BOCHS) += bochs/
 obj-$(CONFIG_DRM_VIRTIO_GPU) += virtio/
 #ifdef VENDOR_EDIT
-obj-$(CONFIG_DRM_MSM) += msm/
+# BailinT: 骨架让位——display 真身在 techpack/display/msm（msm_drm），两套 msm_gem_* 全局符号冲突
+# obj-$(CONFIG_DRM_MSM) += msm/
 #endif
 obj-$(CONFIG_DRM_TEGRA) += tegra/
 obj-$(CONFIG_DRM_STM) += stm/

--- a/techpack/display/config/holidispconf.h
+++ b/techpack/display/config/holidispconf.h
@@ -17,3 +17,8 @@
 #define CONFIG_DRM_OPLUS 1
 #define CONFIG_DRM_OPLUS_NOTIFY 1
 //#endif
+#define CONFIG_V4L2_MEM2MEM_DEV 1
+#define CONFIG_VIDEOBUF2_CORE 1
+#define CONFIG_VIDEOBUF2_V4L2 1
+#define CONFIG_VIDEOBUF2_MEMOPS 1
+#define CONFIG_VIDEOBUF2_DMA_CONTIG 1

--- a/drivers/media/v4l2-core/Makefile
+++ b/drivers/media/v4l2-core/Makefile
@@ -20,7 +20,8 @@
 
 obj-$(CONFIG_VIDEO_TUNER) += tuner.o
 
-obj-$(CONFIG_V4L2_MEM2MEM_DEV) += v4l2-mem2mem.o
+# BailinT: techpack rotator 依赖（无 prompt 符号 defconfig/select 均不可达，直接恒编）
+obj-y += v4l2-mem2mem.o
 
 obj-$(CONFIG_V4L2_FLASH_LED_CLASS) += v4l2-flash-led-class.o
 

--- a/drivers/media/common/videobuf2/Makefile
+++ b/drivers/media/common/videobuf2/Makefile
@@ -5,10 +5,11 @@
   videobuf2-common-objs += vb2-trace.o
 endif
 
-obj-$(CONFIG_VIDEOBUF2_CORE) += videobuf2-common.o
-obj-$(CONFIG_VIDEOBUF2_V4L2) += videobuf2-v4l2.o
-obj-$(CONFIG_VIDEOBUF2_MEMOPS) += videobuf2-memops.o
+# BailinT: techpack rotator 依赖恒编
+obj-y += videobuf2-common.o
+obj-y += videobuf2-v4l2.o
+obj-y += videobuf2-memops.o
 obj-$(CONFIG_VIDEOBUF2_VMALLOC) += videobuf2-vmalloc.o
-obj-$(CONFIG_VIDEOBUF2_DMA_CONTIG) += videobuf2-dma-contig.o
+obj-y += videobuf2-dma-contig.o
 obj-$(CONFIG_VIDEOBUF2_DMA_SG) += videobuf2-dma-sg.o
 obj-$(CONFIG_VIDEOBUF2_DVB) += videobuf2-dvb.o

--- a/techpack/camera/Makefile
+++ b/techpack/camera/Makefile
@@ -36,6 +36,8 @@
 
 ifeq ($(CONFIG_ARCH_YUPIK), y)
 include $(srctree)/techpack/camera/config/yupikcamera.conf
+export CONFIG_SPECTRA_OPLUS=y
+export OPLUS_FEATURE_CAMERA_COMMON=y
 endif
 
 ifeq ($(CONFIG_ARCH_KONA), y)
